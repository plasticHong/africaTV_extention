import oConfig from './modules/config.js';

const oMain = (() => {
    'use strict';

    const template = (() => {
        return {

        };
    })();

    const event = (() => {
        return {

        };
    })();

    return {
        init: () => {

        },
    };
})();

(() => {
    oConfig.init();
    oMain.init();
})();
